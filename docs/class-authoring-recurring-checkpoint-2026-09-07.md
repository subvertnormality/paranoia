# Recurring class-authoring checkpoint — 2026-09-07

The recurring-defect campaign is not merge-qualified. Runtime classification policy
is unchanged. The Codex parent correctly detected both Boolean-as-integer violations,
but authored one MAJOR one-off debt and no class. The experiment therefore cannot
exercise class-preserving repair continuations.

Frozen source: a6ed83f0dcd38d2d017ad560b9a350104502e41e.
Campaign: /tmp/paranoia-recurring-live-2026-09-07.
The defect gates are app.py lines 23 and 75. Each entry point received the existing
100-case identity calibration independently. Both original paths exposed the target;
both exact-type and Boolean-exclusion repairs passed; the half-repaired control
retained the replacement path's Boolean failures. No repair hint was supplied to census.

The native Codex parent completed in 157,036 ms of dispatch using five attempts:
three lanes, one existing same-session anchor-validation retry, and consolidation.
The retry repaired an out-of-range helpers.py citation. The final debt bound both
gates and remained BLOCKED. No class definition was produced or injected.

The complete graph rejects the missing continuation; explicit parent qualification
and both fork gates reject the unqualified authoring assessment. The counter remains
five, and no continuation directory was created. Partial graph inspection validates
custody only and does not grant complete acceptance. The Claude parent and all four
continuations remain unrun; no extra provider spend was warranted after this failure.

Before freeze, the integrated source passed all 2,184 tests in 216.33 seconds and
all 33 owned Protocol v2 mutants, including seven historical V1/V2 differential
groups. These deterministic results do not replace native class-authoring acceptance.

The user approved reopening acceptance to test class authoring directly while
preserving runtime policy. The amended plan separates explicit authoring from the
legitimate optional choice to create a class. Production guidance remains frozen.
Both independently authored provider classes, both repair forms, unchanged class
definitions and normal cold finals remain required. No general speedup is claimed.

[Retained recurring evidence](class-authoring-recurring-evidence-2026-09-07.tar.gz):
121 files, 140,091 compressed bytes, SHA-256
6167c3908c20b5ec647972155f3dc7f6eb5f5b5b464079c0efe482e0867c8864.
Every archive member was compared byte-for-byte with its original run file.
The original one-off result, rejected payload, prompts, channels, native audit,
durable state, fixture calibration and stopped-campaign gate results are retained.
