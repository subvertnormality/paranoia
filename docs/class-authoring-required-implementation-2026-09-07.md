# Required-authoring acceptance implementation

The user-approved Q4f experiment isolates class authoring from the model's legitimate
choice to report a one-off. It does not change runtime classification policy or establish
ordinary class-selection reliability. The four earlier nonqualifying campaigns remain
unchanged.

Codex PLAN convergence used lineage `class-authoring-required-response`: round 1 found
one acceptance gap, round 2 closed it, and the cold final in round 3 was NOT-BLOCKED.
Six calls, no validation retries. The correction requires the exact accepted native
consolidation reply to satisfy the narrowed schema; merely sending that schema is insufficient.
The final reviewed plan is committed at `8493c2b`.

The retained PLAN archive is
`class-authoring-required-plan-evidence-2026-09-07.tar.gz`:
10 files, 18,833 bytes, SHA-256
`00aabd84a5aefa2786f6d4b2fb3e199c028300fd3f8e9c9ffb8dde097b062273`.
Every archived file was compared with the original bytes.

Campaign schema 4 changes only parent consolidation and its existing retry: at least one
finding, with the existing new-class classification alternative. Original/expected role
schemas are sealed before invocation. Actual canonical schemas join native trace digests,
and the native provider parser plus existing protocol decoder validate the accepted reply.
Lane, correction and final schemas remain unchanged.

The deterministic production-handler graph covers both providers, both repairs and cold
finals, with and without a consolidation retry. Its 25 negative controls include all 18
prior controls, five schema controls, and two mixed class/one-off accepted-response controls.
When replaying a native graph without a matching initial-success or retry-success attempt,
the corresponding control is explicitly not applicable there; the deterministic graph
owns that path. No provider call is added to manufacture retry evidence.

Implementation and validation are in progress. No new native class-authoring acceptance,
speed improvement, merge eligibility, or closure of issues 49/50 is claimed by this report.
