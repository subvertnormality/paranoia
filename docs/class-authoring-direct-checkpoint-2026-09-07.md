# Direct class-authoring checkpoint — 2026-09-07

Status: not merge-qualified; four native calls, no continuation spend.
Frozen source: 55ffd2b (complete identity and package hashes in the manifest).
Campaign: /tmp/paranoia-direct-live-2026-09-07.

The exact disclosed authoring task reached only the initial Codex consolidation.
Native invocation hashes, retained full prompt bytes, and the sealed original/augmented
prompt record passed the scope gate. The three census lanes received ordinary production
prompts and correctly detected both Boolean-as-integer defects.

Consolidation again returned one MAJOR one-off finding and no class. Its native reason
states that stable implementation member IDs were not supplied, so it could not honestly
create the reusable definition. Both defects remain blocking. The parent completed in
116,433 ms of dispatch (116,505 ms including terminal qualification), four attempts,
zero retries. This is correct defect detection but failed class-authoring acceptance.

The executable new-definition schema permits model-authored member strings, and the
canonical engine validates and persists them. Later satisfaction must use that durable,
server-supplied inventory exactly. The current consolidation prompt does not make that
ownership transition explicit and also says “Do not invent debt or IDs.” That blanket
wording conflicts with creating a new member inventory. No schema relaxation or forced
runtime class policy is needed to clarify the distinction.

The complete graph rejects missing continuations. Parent qualification and both fork
gates reject the failed authoring assessment without changing the four-call counter or
original terminal evidence. Claude and all repair/final arms remain unrun. No model call
was repeated or silently reclassified as acceptance.

The user has been asked to approve a narrow ownership clarification because the accepted
experiment freezes production guidance. Pending that approval, no new campaign is admitted.
All existing original-class, two-gate, two-repair, immutable-class and cold-final gates remain.

Before freeze, all 2,186 tests passed in 219.47 seconds. The complete scripted graph
exercised the production consolidation validation retry and all 18 resealed negative
controls. The 33-mutation gate's production source, runner and owned tests are identical
to the previously passing a6ed83f checkpoint. These deterministic results do not qualify
the failed native parent.

[Retained direct-authoring evidence](class-authoring-direct-evidence-2026-09-07.tar.gz):
124 files, 157,451 compressed bytes, SHA-256
7ec81f256813bf3eec1e17a0638e8baab7ac96d71117e8f07ef7dbd120190a60.
Every archive member was compared byte-for-byte with its original run file.
This archive includes the task-scope records, native channels, original result and
one-off reason, durable state, fixture calibration and stopped-campaign gate results.
No general performance improvement or closure of issues 49/50 is claimed.
