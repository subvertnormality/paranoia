# Issue 117: bounded fixture continuation

The user approved repairing the native fixture while retaining the original eight
calls and twelve-call ceiling. Source checkpoint 579e580 used the identical complete
production inventory as the original f743559 campaign. No runtime code changed.

The continuation made four actual provider calls: discovery, binding, cold attestation,
and structural correction. All completed without validation retries. The fresh evidence
packet supported the release-date claim using two server-captured authoritative sources.
The correction closed class 79b779c3 and left zero blocking structural debt. Its remaining
minor literal-duplication observation was advisory. Durable structural phase is final.

The attempted round 3 was stopped before provider execution by the global twelve-call
ceiling. Its public result records that error. Consequently no cold final ran and this
campaign is not qualified. The helper's later audit-count assertion also rejected the
missing zero-call audit/trace pair; the retained qualification traceback names that
assertion, while result-3.txt records the preceding ceiling failure. Neither is a clean
review or a failure of the strict attestation parser.

Retained continuation: docs/attestation-envelope-117-continuation-evidence.tar.gz,
59 files, 65,496 bytes, SHA-256
`d7c8dec1d8dd02fed63482e881e895ab3e48cfc1255fa6722899e0859424f4fd`.
Every archived file was compared byte-for-byte with the original terminal directory
at /tmp/paranoia-117-continuation. The separate original eight-call archive and its failed
qualification remain unchanged. Continuation elapsed time was 185,898 ms.

Validation at 579e580: 33 focused tests passed; the full suite passed 2,210 tests in
212.04 seconds. The actual acceptance entry rejects edited parent artifacts and a
changed production inventory. A scripted provider-boundary control admits exactly four
new calls numbered 9 through 12 and rejects the next execution before provider contact.
That control is deterministic test evidence, not native acceptance.

Delivery remains pending. Requested authorization is limited to one cold final and its
existing single validation retry, at most fourteen cumulative calls, preserving both
failed qualification archives, exact production binding, native timeouts, supported
evidence, durable reload, structural clearance and subsequent Codex CODE convergence.
No restart, new fixture repair, fresh census or counter reset is proposed. Main and the
active review-quality-speed branch have not yet received issue 117.
