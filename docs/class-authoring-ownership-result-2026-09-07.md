# Ownership clarification: native result and checkpoint

Source 791e33ce590e6f75bbfff5f61ef864260ed41903 includes the delivered issue 117 fix
and the approved new/replacement member-ID ownership clarification. The frozen source
worktree is .worktrees/class-authoring-ownership-source. The native campaign is
/tmp/paranoia-ownership-live-2026-09-07.

Codex p01 detected both original Boolean-as-integer defects and consolidated them into
one MAJOR finding. It nevertheless classified the finding as one_off, with the reason
"Both validators contain the same localized boolean-index acceptance bug." No reusable
class was authored. This is defect detection, not class-authoring acceptance. The exact
model-visible prompt included both the direct authoring task and the approved ownership
guidance. Consolidation described exact-type syntax as one compliant repair, which is
consistent with the intended behavioral guidance, but no class exists to test closure.

The parent made four calls with zero retries. Dispatch took 125,834 ms; terminal elapsed
time was 125,973 ms. The native consolidation session is
01a07d67-6521-73a0-8a05-1c14c8148c65. The qualified custody graph accounts for all four
calls. A sealed negative assessment makes qualify_parent reject with
"target authoring was not qualified". No dependent calls, repair forks, or Opus parent
were run. Neither classes nor native state were edited to make the test pass.

The complete retained archive is class-authoring-ownership-evidence-2026-09-07.tar.gz:
123 files, 158,665 bytes, SHA-256
`d008a9956023703d09c8c3196cf2aebb014bc561718e63bb5442fbc3efaf8f27`.
Every archived file was compared with the terminal campaign bytes. Earlier three
nonqualifying campaigns remain unchanged.

A zero-call schema control reconstructed the source-bound census schema. Its provider
projection digest exactly matches the native trace:
`526d382517d1aa1b0bec003ae3fe2acdbdb87c2165a9a8b88daa40d5e27b92c1`.
Both that projection and the full local schema admit a counterfactual unmechanized
new-class definition with independently authored member IDs. The counterfactual was
never submitted, persisted or treated as native acceptance. This rules out a missing
new-class wire alternative; it does not establish why the model preferred one-off debt.

The current full suite passed 2,224 tests during issue 117 integration. The plan's
existing staged-protocol mutation gate also passed: all 33 owned mutants were killed,
with its historical V1/V2 differential gate retained.

Architecture checkpoint: the ownership wording is insufficient for the frozen native
acceptance. Do not merge this candidate, rerun the unchanged parent, inject a class,
weaken runtime classification or weaken either alternative-repair gate. The experiment
still conflates optional class selection with the quality of an authored class. The
available evidence does not justify moving semantic ownership between production roles
or introducing another reviewer call. A proposed test-only design change is recorded
separately for approval. Q4/Q5 and the later general-speed investigation remain incomplete.
