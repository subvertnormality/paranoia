# Bounded correction: evidence justifications, not private reasoning

Scope: the shared ATTESTATION_OUTPUT_INSTRUCTIONS in handlers.py asks for "all reasoning" although its closed output contract needs only short evidence-based justifications for two independently judged booleans. A native run failed with a provider safeguard refusal tagged reasoning_extraction. This is evidence of failure, not proof that this sentence caused it. Do not disable safeguards, change providers or attempt repeated evasive prompt variants.

Replace that request with a request for concise evidence-based justifications in authority_reason and entailment_reason, explicitly excluding private internal reasoning and step-by-step deliberation. Preserve exact field names, required nonempty values and existing character bound, independent publisher-authority and passage-entailment judgments, complete identity coverage, strict one-envelope parsing, same-session single validation retry, cold reviewer configuration, capture and structural gates. Both initial and correction renderers continue to share the same contract. Production scope is this shared instruction text and the minimal shared attestation-parser correction specified below; all other production behavior is unchanged.

Before editing production, add a regression through both actual prompt renderers requiring concise evidence-based justifications and excluding requests for private reasoning; preserve all existing contract/schema and reply-validation tests. Observe RED, make the minimal text correction, run focused tests. Record why this is a contract clarification rather than weaker evidence adjudication.

Historical provider records and the previous failed native run remain unchanged. Update only existing later-source allowance metadata affected by the instruction difference, retaining original field sets, sources, prompts, outcomes, and identity values. Existing immutability and original-validator mutation controls must pass. Explicitly document that historical runs did not exercise this current wording and do not constitute its native acceptance. Current-source provider qualification requires CURRENT-WORDING-QUALIFIED below.

Review this bounded design with Codex under the existing issue126 stakes, then implement under TDD and run Codex CODE convergence on the updated branch with this exact contract. Do not rerun PLAN after implementation starts. Keep prior issue126 repair and evidence intact; the existing blocked draft PR may include this correction only with actual updated review/test status.

Run the complete canonical Linux test tree with no exclusions before any push. Native acceptance then uses the source-bound launcher, same configured Claude roles/model, preserved predecessor state, and the corrected private test design (adding its named facade observation comparator), at the next legal round label. Retain every attempt and actual verdict. Native success requires CURRENT-WORDING-QUALIFIED below in addition to the existing receipt gates; another provider refusal or unresolved required review blocks dependent implementation. Never remove assertions because attestation failed, reset lineage, weaken review profiles or start the campaign.


## CURRENT-WORDING-QUALIFIED: sole native qualification authority

Extend scripts/run_claim_admission_acceptance.py:validate_receipt and its tests, in addition to the instruction text and shared parser correction named in this contract. Require current-run native attestation evidence from the same source-bound audit: at least one claim-attestation initial attempt, with a completed initial reply or its existing completed same-session claim-attestation-validation-retry. Retain and apply every existing execution-failure, unrepaired-validation, identity, predecessor and structural-result gate. Require the final claim audit to be parsed and not failed as before. A discovery-only run, absence of current attestation, historical attestation metadata, or terminally rejected attestation cannot qualify.

This witness uses the verified production path rather than introducing a second parser or instrumentation framework: _CapturedClaimEngine._attest invokes the native attester only for a nonempty packet; its corrected shared parser rejects ambiguous envelopes and checks exact response coverage and schema, and marks invalid replies in the current attempt ledger. Source identity binds the actual shared instruction used by both renderers. Successful final parsing plus repaired-only ledger validation and a current attestation attempt therefore demonstrate nonempty current-wording execution and locally validated completion. Retain the actual current audit and all native channels; do not infer qualification from historical claims, source presence alone or successful discovery.

Add named tests for valid nonempty production-shaped attestation, successful same-session attestation correction, rejection of discovery-only and missing-attestation receipts, and rejection of terminally invalid/failed attestation. Include an integration control through the actual _attest adapter that proves an empty inventory does not invoke an attester and cannot qualify, while a nonempty packet with a valid complete reply invokes the shared current instruction and qualifies. Existing native evidence remains immutable. Missing qualification retains failure and blocks readiness, not the opportunity for later authorized testing.


## Shared parser correction required before qualification

The existing parse_attestation closure inside _CapturedClaimEngine._attest is the sole parser for initial replies and their single same-session correction. Extend that parser only: reject any non-whitespace prefix before its marker, require a complete JSON object after the marker with no trailing content, and reject duplicate JSON object keys at every nesting depth before constructing the final mapping. Use a local object_pairs_hook with the standard JSON decoder; do not add a second acceptance parser, change staged-protocol or engine parsing, or broaden Unicode acceptance/rejection. Keep the current reply-size, exact envelope/row fields, identity coverage, boolean verdicts, nonempty bounded justifications and remaining validation checks.

Extend tests/test_issue117.py:test_public_claude_attestation_correction with preceding conflicting content, duplicate envelope keys and duplicate row/verdict keys. Exercise each on the initial response and on the correction, including a valid same-session repair, an independently negative valid verdict, and terminal rejection. The public adapter must retain initial rejected channels, use only its existing retry, preserve prior history, and keep terminal failure blocking. Feed terminal-invalid and successful repaired actual attempt ledgers into the acceptance receipt controls; only the latter can meet CURRENT-WORDING-QUALIFIED. Existing tests for valid envelopes, size bounds, extra keys, identity coverage and multiple envelopes remain unchanged apart from adding the new cases.

This is a prerequisite correction to the claimed unambiguous-envelope guarantee, not a new review framework. No assertion is removed, no provider safeguard is disabled, and no incomplete native evidence is credited.
